{
  "_comment": "Example Claude-Code settings.json hooks fragment. JSON has no comments, so notes live in _comment keys (Claude Code ignores unknown keys). Merge the hooks block into your own settings.json.",
  "hooks": {
    "SessionStart": [
      {
        "_comment": "Copy `behavior-layer.example.md` into your own vault as `behavior-layer.md` (e.g. at your project root) and point this command at it. `${CLAUDE_PROJECT_DIR}` resolves to your project root at runtime. On Windows, emit no-BOM UTF-8 so non-ASCII echoes safely (a leading BOM can corrupt the first injected line).",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -Command \"[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false); Get-Content -Raw -Encoding utf8 '${CLAUDE_PROJECT_DIR}/behavior-layer.md'\""
          }
        ]
      }
    ],
    "_comment_PreToolUse": "A PreToolUse command hook can validate a tool argument against an allowlist and BLOCK the call (exit non-zero) before it runs — e.g. guarding a tag / label set so only an approved vocabulary is ever written. Point its command at your own validator script. Shown here as a commented pattern, not active.",
    "_PreToolUse_pattern": [
      {
        "matcher": "<your-tool-name>",
        "hooks": [
          {
            "type": "command",
            "command": "<path-to-your-allowlist-guard-script>"
          }
        ]
      }
    ]
  }
}
