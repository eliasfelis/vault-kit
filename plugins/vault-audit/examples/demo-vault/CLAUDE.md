# Demo Vault

A tiny fake vault used to self-test vault-audit. Its rules live in `rules.yaml`.

## Folder routing

- `notes/` — general notes
- `projects/` — one folder per project
- `inbox/` — capture, cleared weekly
- `widgets/` — reusable widgets
